// Compilation unit for nvcc — includes the full Eigen-based generic implementations.
// Compile with: nvcc -rdc=true -fatbin -I<eigen_include> -I<include> -arch=sm_XX this_file.cu
#include "generic.cuh"
